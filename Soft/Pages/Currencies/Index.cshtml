@page
@model ABC.Pages.Party.CurrenciesPages;

@{
    Layout = "_Index";
}
<table class="table">
    <thead>
    <tr>
        <th>
            <a asp-page="./Index"
               asp-page-handler="Index"
               asp-route-order="@Model.SortOrder(nameof(Model.Item.Name))"
               asp-route-idx="@Model.PageIndex"
               asp-route-filter="@Model.CurrentFilter"
            >@nameof(Model.Item.Name)</a>
        </th>
        <th>
            <a asp-page="./Index"
               asp-page-handler="Index"
               asp-route-order="@Model.SortOrder(nameof(Model.Item.Description))"
               asp-route-idx="@Model.PageIndex"
               asp-route-filter="@Model.CurrentFilter"
            >@nameof(Model.Item.Description)</a>
        </th>
        <th>
            <a asp-page="./Index"
               asp-page-handler="Index"
               asp-route-order="@Model.SortOrder(nameof(Model.Item.Code))"
               asp-route-idx="@Model.PageIndex"
               asp-route-filter="@Model.CurrentFilter"
            >@nameof(Model.Item.Code)</a>
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Items) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Code)
        </td>
        
        <td>
            <a asp-page="./Edit"
               asp-page-handler="Edit" 
               asp-route-id="@item.Id"
               asp-route-order="@Model.CurrentSort"
               asp-route-idx="@Model.PageIndex"
               asp-route-filter="@Model.CurrentFilter"
            >Edit</a>&nbsp 

            <a asp-page="./Details"
               asp-page-handler="Details"
               asp-route-id="@item.Id" 
               asp-route-order="@Model.CurrentSort"
               asp-route-idx="@Model.PageIndex"
               asp-route-filter="@Model.CurrentFilter"
            >Details</a>&nbsp 

            <a asp-page="./Delete" 
               asp-page-handler="Delete" 
               asp-route-id="@item.Id"
               asp-route-order="@Model.CurrentSort"
               asp-route-idx="@Model.PageIndex"
               asp-route-filter="@Model.CurrentFilter"
            >Delete</a>
        </td>
    </tr>
}
    </tbody>
</table>
